# Upstream code kept verbatim (see lua/md-render/vendor/README.md). Marking it
# vendored keeps it out of the repository's language statistics and collapses
# it in diffs, so review sees only what this project actually wrote.
lua/md-render/vendor/** linguist-vendored
